import React, {Component} from 'react';
import { View, Text,} from 'react-native';

export default class Login extends Component {
    render() {
        return (
            <View tyle={{flex:1,backgroundColor: 'red'}}><Text>Login</Text></View>
        )
    }
}